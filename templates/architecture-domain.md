---
artifact: android-architecture-domain
version: 1.0
owner: spec-kit
last_updated_by_task: <task_id>
---

# <Domain name>

<One-sentence purpose: what part of the system this file describes. Domain names are project-specific — e.g. networking.md, billing.md, navigation.md, data-layer.md. Pick the name that matches how this codebase's own modules/packages are already organized.>

## Owner

<Team / lane / individual responsible for this domain, if known. Otherwise: "Spec Kit (no dedicated owner)".>

## Tech stack

<Key libraries, frameworks, or architectural patterns this domain relies on — e.g. Retrofit + OkHttp, Room, Hilt, Compose Navigation.>

## References

<Links to related ADRs in docs/ai/decisions/ that justify decisions described below — e.g. "See [ADR-0003](../decisions/ADR-0003-network-layer-choice.md)". Every non-obvious decision mentioned in the body sections below should trace back to an ADR here, not be re-argued inline.>

## <Body section 1 — free-form, name per domain>

<What this part of the system does, its boundaries, and how other domains call into it. Written for a developer joining the project, not as a code comment — prose, not a dump of the architecture graph.>

## <Body section 2 — add as many as the domain needs>

## Does NOT do

<Explicit boundary — what a developer might assume this domain handles but does not. Prevents scope creep and misplaced changes.>

## Change Log

**Section-level patch only.** When Stage 7 updates this file, it must rewrite only the body section(s) actually affected by the completed task, leave every other section byte-for-byte untouched, and append exactly one row below — never rewrite the whole file.

| Date | Task | Change |
|---|---|---|
